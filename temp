import re
import dataiku

MAIN_FOLDER = "LLM_BIREYSEL"
mf = dataiku.Folder(MAIN_FOLDER)

def tum_yollar(folder):
    """Folder icindeki tum dosya yollarini recursive olarak doner."""
    try:
        yollar = folder.list_paths_in_partition()
        if yollar:
            return yollar
    except Exception:
        pass

    bulunan = []

    def _gez(p):
        try:
            det = folder.get_path_details(path=p)
        except Exception:
            return
        for c in det.get("children", []):
            tam = c.get("fullPath")
            if not tam:
                continue
            if c.get("directory"):
                _gez(tam)
            else:
                bulunan.append(tam)

    _gez("/")
    return bulunan

def oku(folder, path):
    with folder.get_download_stream(path) as f:
        return f.read().decode("utf-8", errors="ignore")

def feature_bolumunu_cek(md_text):
    """## Onerilen Feature basligindan bir sonraki ## basligina (veya dosya sonuna) kadar olan kismi doner."""
    pattern = r"##\s*Önerilen Feature\s*\n(.*?)(?=\n##\s|\Z)"
    match = re.search(pattern, md_text, flags=re.DOTALL)
    if match:
        return match.group(1).strip()
    return None

tum_yollar_listesi = tum_yollar(mf)

# Sadece .../hypotheses/H*.md dosyalarini filtrele
hipotez_dosyalari = [
    p for p in tum_yollar_listesi
    if "/hypotheses/" in p and re.search(r"/H\d+\.md$", p, flags=re.IGNORECASE)
]

sonuclar = []  # her biri: {"grup": ..., "dosya": ..., "feature": ...}

for path in sorted(hipotez_dosyalari):
    parcalar = path.strip("/").split("/")
    grup = parcalar[0]          # LLM_BIREYSEL/<grup>/hypotheses/H01.md -> <grup>
    dosya_adi = parcalar[-1]    # H01.md

    icerik = oku(mf, path)
    feature_kismi = feature_bolumunu_cek(icerik)

    sonuclar.append({
        "grup": grup,
        "dosya": dosya_adi,
        "feature": feature_kismi
    })

# Okunabilir sekilde yazdir
for s in sonuclar:
    print(f"\n{'='*80}\n[{s['grup']}] {s['dosya']}\n{'='*80}")
    if s["feature"]:
        print(s["feature"])
    else:
        print("(Önerilen Feature bölümü bulunamadı)")
