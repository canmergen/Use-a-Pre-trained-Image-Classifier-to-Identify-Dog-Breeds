def metric_tipi(fam: str, m: str) -> str:
    tam = f"{fam}_{m}"
    if "RATIO" in tam:
        return "ORAN"
    if m.endswith("SHR"):
        return "PAY"
    if m in ("TMSNCFRST", "TMSNCLST"):
        return "SÜRE"
    return "değer"
