periods_sorted = sorted(df["PERIOD"].unique())

# Son 4 dönem OOT
oot_periods = periods_sorted[-4:]
train_pool_periods = periods_sorted[:-4]

# Train pool içinden dönem bazlı %80-20 train/val ayrımı
n_train = round(len(train_pool_periods) * 0.8)

train_periods = train_pool_periods[:n_train]
val_periods = train_pool_periods[n_train:]

print("OOT donemler:", oot_periods)
print("Train donemler:", train_periods)
print("Val donemler:", val_periods)

df_train = df[df["PERIOD"].isin(train_periods)].copy()
df_val   = df[df["PERIOD"].isin(val_periods)].copy()
df_oot   = df[df["PERIOD"].isin(oot_periods)].copy()

print("Train shape:", df_train.shape, "| donem sayisi:", len(train_periods))
print("Val shape:", df_val.shape, "| donem sayisi:", len(val_periods))
print("OOT shape:", df_oot.shape, "| donem sayisi:", len(oot_periods))
