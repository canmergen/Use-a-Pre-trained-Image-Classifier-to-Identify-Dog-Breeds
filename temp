# --- Alt klasörü oluştur ve kontrol et ---
readme = f"""3M_RENK_GOC - model ciktilari
TRAIN_DONEM : {TRAIN_DONEM}
OOT_DONEM   : {OOT_DONEM}
"""

with mf.get_writer(PATH + "/readme.txt") as w:
    w.write(readme.encode("utf-8"))

print("Yazildi:", PATH + "/readme.txt")
print("Klasor icerigi:", [p for p in mf.list_paths_in_partition() if p.startswith(PATH + "/")])
