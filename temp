# ============================================================
# Her renk için train / VAL / OOT pozitif (batan) SAYILARI ve ORANLARI
# ============================================================
# TRAIN_DONEM artık ikiye bölünüyor (train %80 / val %20, stratified).
# Buradaki val kırılımı gerçek bölmeyle BİREBİR aynı sayıları verir:
# stratified split'te sınıf başına düşen adet yalnızca n ve sınıf
# sayılarına bağlıdır — hangi satırların seçildiğinden bağımsızdır.
# VAL_ORAN ve RANDOM_STATE diğer bloklarla aynı olmalı.

from sklearn.model_selection import train_test_split

MODEL_RENKLER = [1, 2, 3, 4, 5, 6]
VAL_ORAN = 0.20

print(f"{'renk':<14} | {'train n':>10} {'bad':>9} {'oran':>7} | "
      f"{'val n':>9} {'bad':>8} {'oran':>7} | "
      f"{'oot n':>10} {'bad':>9} {'oran':>7}")
print("-" * 104)

for RENK_KOD in MODEL_RENKLER:
    renk_adi = COLOR_MAP.get(RENK_KOD, str(RENK_KOD))
    m = ((df['KK_URUN_FLG'] == 'KK') & (df['RENK'] == RENK_KOD) &
         (df['DONEM'].isin([TRAIN_DONEM, OOT_DONEM])))
    d  = df.loc[m]
    tf = d[d['DONEM'] == TRAIN_DONEM]      # train_full (bölünmeden önce)
    ot = d[d['DONEM'] == OOT_DONEM]

    ot_bad  = int(ot[TARGET].sum())
    ot_rate = ot[TARGET].mean() if len(ot) else 0.0   # ← bucket lift'inin paydası

    # train / val bölmesi — pipeline ile aynı parametreler
    if len(tf) and tf[TARGET].nunique() >= 2:
        _tr, _val = train_test_split(tf, test_size=VAL_ORAN,
                                     stratify=tf[TARGET], shuffle=True,
                                     random_state=RANDOM_STATE)
    else:
        _tr, _val = tf, tf.iloc[0:0]

    tr_bad,  val_bad  = int(_tr[TARGET].sum()), int(_val[TARGET].sum())
    tr_rate  = _tr[TARGET].mean()  if len(_tr)  else 0.0
    val_rate = _val[TARGET].mean() if len(_val) else 0.0

    print(f"{RENK_KOD}-{renk_adi:<12} | {len(_tr):>10,} {tr_bad:>9,} {tr_rate:>7.2%} | "
          f"{len(_val):>9,} {val_bad:>8,} {val_rate:>7.2%} | "
          f"{len(ot):>10,} {ot_bad:>9,} {ot_rate:>7.2%}")

    # Uyarı: val'de pozitif sayısı çok düşükse gini/eşik gürültülü olur.
    if 0 < val_bad < 100:
        print(f"{'':<14}   ⚠ val'de sadece {val_bad} pozitif — "
              f"val_gini ve eşik güvenilmez olabilir")
