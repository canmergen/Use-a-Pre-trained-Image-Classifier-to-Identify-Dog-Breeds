import numpy as np
import pandas as pd

JOIN_KEYS = ["DONEM", "MBB_NO"]

TARGET_COLS = [
    "TARGET_RENK3MSONRASIKIRMIZI",
    "TARGET_RENK6MSONRASIKIRMIZI",
    "TARGET_RENK12MSONRASIKIRMIZI",
    "TARGET_TAKIP6M",
]

df2_slim = df2[JOIN_KEYS + TARGET_COLS].copy()

# 1) Anahtar tiplerini iki tarafta da eşitle (float 202506.0 vs int 202506 join'i sessizce boşa düşürür)
for col in JOIN_KEYS:
    df1[col] = pd.to_numeric(df1[col], errors="coerce").astype("Int64")
    df2_slim[col] = pd.to_numeric(df2_slim[col], errors="coerce").astype("Int64")

# 2) Ortak dönemleri bul ve İKİ TARAFI DA bu dönemlerle sınırla
donem_df1 = set(df1["DONEM"].dropna().unique())
donem_df2 = set(df2_slim["DONEM"].dropna().unique())
ortak_donemler = sorted(donem_df1 & donem_df2)

print("df1 dönemleri:", sorted(donem_df1))
print("df2 dönemleri:", sorted(donem_df2))
print("ortak dönemler:", ortak_donemler)
print("df1'de olup df2'de olmayan:", sorted(donem_df1 - donem_df2))
print("df2'de olup df1'de olmayan:", sorted(donem_df2 - donem_df1))

df1_f = df1[df1["DONEM"].isin(ortak_donemler)].copy()
df2_f = df2_slim[df2_slim["DONEM"].isin(ortak_donemler)].copy()

# 3) df2 tarafı anahtar bazında tekil mi? Değilse left join df1'i çoğaltır
dup_cnt = df2_f.duplicated(subset=JOIN_KEYS).sum()
print("df2 mükerrer anahtar satırı:", dup_cnt)
if dup_cnt > 0:
    df2_f = df2_f.drop_duplicates(subset=JOIN_KEYS, keep="first")

# 4) Left join — eşleşmeyen MBB_NO'lar NaN kalır
n_before = len(df1_f)

df_merged = df1_f.merge(
    df2_f,
    on=JOIN_KEYS,
    how="left",
    validate="m:1",
    indicator="_merge_flg",
)

# 5) Kontroller
print("df1 (ortak dönem):", n_before, "| merge sonrası:", len(df_merged))
print(df_merged["_merge_flg"].value_counts())
print("eşleşmeyen oran:", (df_merged["_merge_flg"] == "left_only").mean())

# 6) Target'lar NaN'ı koruyarak integer kalsın (int8 değil, nullable Int8)
for col in TARGET_COLS:
    df_merged[col] = df_merged[col].astype("Int8")

df_merged = df_merged.drop(columns=["_merge_flg"])
