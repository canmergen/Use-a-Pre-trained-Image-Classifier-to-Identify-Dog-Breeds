import numpy as np
import pandas as pd

JOIN_KEYS = ["DONEM", "MBB_NO"]

# df2'den sadece join anahtarları + taşımak istediğin kolonlar
TARGET_COLS = [
    "TARGET_RENK3MSONRASIKIRMIZI",
    "TARGET_RENK6MSONRASIKIRMIZI",
    "TARGET_RENK12MSONRASIKIRMIZI",
    "TARGET_TAKIP6M",
]

df2_slim = df2[JOIN_KEYS + TARGET_COLS].copy()

# 1) Anahtar tiplerini iki tarafta da aynı yap (float 202506.0 vs int 202506 join'i sessizce boş getirir)
for col in JOIN_KEYS:
    df1[col] = pd.to_numeric(df1[col], errors="coerce").astype("Int64")
    df2_slim[col] = pd.to_numeric(df2_slim[col], errors="coerce").astype("Int64")

# 2) df2 anahtar bazında tekil mi? Değilse left join satır sayısını şişirir
dup_cnt = df2_slim.duplicated(subset=JOIN_KEYS).sum()
print("df2 mükerrer anahtar satırı:", dup_cnt)

if dup_cnt > 0:
    # Tekilleştirme gerekiyorsa (ilk satırı al):
    df2_slim = df2_slim.drop_duplicates(subset=JOIN_KEYS, keep="first")

# 3) Left join
n_before = len(df1)

df_merged = df1.merge(
    df2_slim,
    on=JOIN_KEYS,
    how="left",
    validate="m:1",      # df2 tarafı tekil değilse hata fırlatır, sessizce şişmez
    indicator="_merge_flg",
)

# 4) Kontroller
print("df1 satır:", n_before, "| merge sonrası:", len(df_merged))
print(df_merged["_merge_flg"].value_counts())
print("eşleşmeyen oran:", (df_merged["_merge_flg"] == "left_only").mean())

# 5) Eşleşmeyenlerde target'lar NaN kalır; int8 istiyorsan doldur
for col in TARGET_COLS:
    df_merged[col] = df_merged[col].fillna(0).astype("int8")

df_merged = df_merged.drop(columns=["_merge_flg"])
