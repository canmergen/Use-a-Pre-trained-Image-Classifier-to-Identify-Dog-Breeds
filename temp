# Herhangi bir renk için, iki feature listesiyle satır kümesini karşılaştır
mask = ((df['KK_URUN_FLG']=='KK') & (df['RENK']==1) & (df['DONEM']==OOT_DONEM))
df_e, _ = feature_engineering(df.loc[mask].copy())

f_kir = secilen_modeller[1]['features']                      # kırmızının listesi
f_tak = _takip_model_ayarlari(1, COLOR_MAP[1])[0]            # takibin listesi
f_tak = [c for c in f_tak if c in df_e.columns]

a = preprocess(df_e[f_kir + [TARGET]])
b = preprocess(df_e[f_tak + [TAKIP_COL]])
print("kırmızı:", len(a), "| takip:", len(b),
      "| simetrik fark:", len(set(a.index) ^ set(b.index)))
