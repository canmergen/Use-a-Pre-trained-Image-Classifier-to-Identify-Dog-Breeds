import re

ARANACAK_GRUP = "bireysel_v7_qwen36_v7"   # <- grup adında aranacak kelime

filtrelenmis = [
    s for s in sonuclar
    if ARANACAK_GRUP.lower() in s["grup"].lower()
    and re.match(r"^H\d+\.md$", s["dosya"], flags=re.IGNORECASE)
]

print(f"Eşleşen kayıt sayısı: {len(filtrelenmis)}\n")

for s in filtrelenmis:
    print(f"{'='*80}\n[{s['grup']}] {s['dosya']}\n{'='*80}")
    print(s["feature"] if s["feature"] else "(Önerilen Feature bölümü bulunamadı)")
    print()
