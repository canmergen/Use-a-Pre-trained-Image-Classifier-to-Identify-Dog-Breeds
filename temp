import dataiku

mf_test = dataiku.Folder("MODEL_RESULTS")

# 1) Nokta ile başlayan, boş dosya
with mf_test.get_writer("/TEST_A/.keep") as w:
    w.write(b"")

# 2) Normal isimli, içerikli dosya
with mf_test.get_writer("/TEST_B/_klasor.txt") as w:
    w.write(b"klasor isareti\n")

mf_test = dataiku.Folder("MODEL_RESULTS")   # handle yenile
print("Listeleme:", mf_test.list_paths_in_partition())
