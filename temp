# --- 1.9b: Bu koşunun kabullerini insan diliyle özetle ---
YON_TR   = {"yuksek_riskli": "değer yükseldikçe risk artıyor",
            "dusuk_riskli": "değer yükseldikçe risk azalıyor"}
SEKIL_TR = {"monoton_artan": "düzenli artış (yön güvenilir)",
            "monoton_azalan": "düzenli azalış (yön güvenilir)",
            "ters_U_orta_dilimler_riskli": "ters-U: en riskliler ORTA dilimlerde",
            "U_uclar_riskli": "U: en riskliler UÇ dilimlerde",
            "karisik": "düzensiz desen"}

for k in kosu_kabul:
    i = k["istatistikler"]
    print(f"\n{'='*70}\n{k['ad']}{'  [ZAYIF SİNYAL]' if k['zayif_sinyal'] else ''}")
    print(f"  Fikir   : {k['gerekce']}")
    print(f"  Formül  : {k['formul']}")
    print(f"  En riskli %10'luk grupta batma: %{i['en_riskli_dilim_rate']*100:.1f} "
          f"(genel %{i['base_rate']*100:.0f} → {i['pp_gap']:+.1f} puan, {i['lift']:.2f} kat)")
    print(f"  Eğri    : {SEKIL_TR.get(i['egri_sekli'], i['egri_sekli'])}")
    print(f"  Yön     : {YON_TR.get(i['gozlenen_yon'], '?')}"
          + (f"  |  NOT: {k['yon_notu']}" if k['yon_notu'] else ""))
    print(f"  Sıfır oranı: %{i['sifir_orani']*100:.0f} | Öncekilerle benzerlik: "
          f"{i['mevcutlarla_max_korelasyon']}")
    print(f"  Dilim eğrisi (düşük→yüksek): {i['decile_egrisi']}")
