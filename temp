# --- TARGET'ı sayısala çevir (object -> int8) ---
df[TARGET] = pd.to_numeric(df[TARGET], errors="raise").astype("int8")

display(df[TARGET].value_counts().map("{:,}".format))

renk_map_ters = {v: k for k, v in renk_map.items()}
renk_txt = df["RENK"].map(renk_map_ters)

fmt = {"toplam": "{:,.0f}", "kirmizi": "{:,.0f}", "oran": "{:.2f}"}

g = df.groupby(renk_txt)[TARGET].agg(toplam="size", kirmizi="sum")
g["oran"] = (g["kirmizi"] / g["toplam"] * 100).round(2)
display(g.style.format(fmt))

f = df.groupby(["DONEM", renk_txt])[TARGET].agg(toplam="size", kirmizi="sum")
f["oran"] = (f["kirmizi"] / f["toplam"] * 100).round(2)

f_show = f.copy()
lv = f_show.index.levels[0].astype("int64").astype(str)
f_show.index = f_show.index.set_levels(lv.str[:4] + "-" + lv.str[4:], level=0)
display(f_show.style.format(fmt))
