# → Mini'ye (dataset) yaz  [alternatif: folder CSV]
# → Dataset varsa güncelle, yoksa folder'a CSV yaz
SCORED_DATASET = "RENK_MDL_SCORED"
_client  = dataiku.api_client()
_project = _client.get_project(dataiku.default_project_key())
_mevcut  = {d['name'] for d in _project.list_datasets()}

if SCORED_DATASET in _mevcut:
    dataiku.Dataset(SCORED_DATASET).write_with_schema(scored)
    print(f"✓ {SCORED_DATASET} dataseti güncellendi ({scored.shape[0]:,} satır)")
else:
    _fname = "RENK_MDL_SCORED.csv"
    _yaz(_fname, scored, chunk=500_000)      # PATH alt klasörüne akıtarak yazar
    print(f"✓ Dataset bulunamadı → {PATH}/{_fname} yazıldı "
          f"({scored.shape[0]:,} satır)")
    print("  İpucu: Dataiku UI'da 'RENK_MDL_SCORED' adlı dataset oluşturup "
          "tekrar çalıştırırsanız dataset'e yazılır.")
