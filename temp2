import os, tempfile
import numpy as np

OUT_NAME = "model_data.parquet"
hedef    = f"{PATH}/{OUT_NAME}"

out = df.copy()

# val_index eşleşmesi için ham satır sırası
if "SRC_INDEX" not in out.columns:
    out = out.reset_index(drop=True)
    out["SRC_INDEX"] = np.arange(len(out), dtype=np.int64)

# dosya boyutunu yarıya indirir
korunacak = {"SRC_INDEX", "MBB_NO", "DONEM"}
for c in out.columns:
    if c not in korunacak and out[c].dtype == np.float64:
        out[c] = out[c].astype(np.float32)

tmp = os.path.join(tempfile.gettempdir(), OUT_NAME)
out.to_parquet(tmp, engine="pyarrow", compression="snappy", index=False)

with open(tmp, "rb") as f:
    mf.upload_stream(hedef, f)

print(f"{hedef} | {out.shape[0]:,} satır x {out.shape[1]} kolon | "
      f"{round(os.path.getsize(tmp)/1024**2, 1)} MB")
if "DONEM" in out.columns:
    print(out["DONEM"].value_counts().sort_index().to_string())

os.remove(tmp)
del out
